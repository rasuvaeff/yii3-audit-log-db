{
    "$schema": "vendor/infection/infection/resources/schema.json",
    "source": {
        "directories": ["src"]
    },
    "logs": {
        "text": "php://stderr",
        "summary": "build/infection-summary.log"
    },
    "testFramework": "testo",
    // 1 equivalent mutant: AuditEventSerializer::utc() AssignCoalesce (??= → =)
    // always returns DateTimeZone('UTC') either way — caching is unobservable.
    "minMsi": 96,
    "mutators": {
        "@default": true
    }
}
